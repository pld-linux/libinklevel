--- libinklevel/Makefile.orig	2006-07-16 12:06:39.000000000 +0200
+++ libinklevel/Makefile	2006-07-22 00:10:17.024844000 +0200
@@ -1,17 +1,17 @@
-DESTDIR = /usr/local/
-CFLAGS = -Wall -O2 -fPIC -DPIC -I.
+DESTDIR =
+LIBDIR = /usr/lib
+CFLAGS = $(OPTFLAGS) -Wall -fPIC -DPIC -I.
 
 all: libinklevel.so.2.0.6.5
 
 install: all
-	mkdir -p $(DESTDIR)/include
-	install -m 644 -o root -g root inklevel.h $(DESTDIR)/include/inklevel.h
-	mkdir -p $(DESTDIR)/lib
-	install -m 755 -o root -g root libinklevel.so.2.0.6.5 \
-	$(DESTDIR)/lib/libinklevel.so.2.0.6.5
-	cd $(DESTDIR)/lib && rm -fr libinklevel.so && \
+	mkdir -p $(DESTDIR)/usr/include
+	install -m 644 inklevel.h $(DESTDIR)/usr/include/inklevel.h
+	mkdir -p $(DESTDIR)$(LIBDIR)
+	install -m 755 libinklevel.so.2.0.6.5 \
+	$(DESTDIR)$(LIBDIR)/libinklevel.so.2.0.6.5
+	cd $(DESTDIR)$(LIBDIR) && rm -fr libinklevel.so && \
 	ln -s libinklevel.so.2.0.6.5 libinklevel.so
-	ldconfig $(DESTDIR)/lib
 
 clean:
 	rm -fr *.o *.so.2.0.6.5 *~
